<button
  type="button"
  data-font-toggle
  class="font-toggle"
  title="切換為黑體"
>
  <span class="font-icon" aria-hidden="true">文</span>
  <span data-font-label>黑體</span>
</button>
<span class="visually-hidden" data-font-status aria-live="polite">
  目前使用 Iansui 襯體
</span>

<style>
  .font-toggle {
    display: inline-flex;
    min-block-size: 2rem;
    align-items: center;
    justify-content: center;
    border: 1px solid transparent;
    border-radius: 999px;
    padding: 0.375rem 0.75rem;
    background: var(--accent-soft);
    color: var(--muted-foreground);
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    line-height: 1.4;
    white-space: nowrap;
    cursor: pointer;
    transition: color 150ms ease, background-color 150ms ease;
  }
  .font-toggle:hover {
    background: var(--accent-soft-strong);
    color: var(--primary);
  }
  .font-toggle:focus-visible {
    outline: 2px solid var(--ring);
    outline-offset: 3px;
  }
  .font-icon {
    margin-inline-end: 0.375rem;
    font-family: var(--font-reading);
    font-size: 0.8125rem;
    line-height: 1;
  }
  .visually-hidden {
    position: absolute;
    inline-size: 1px;
    block-size: 1px;
    overflow: hidden;
    clip-path: inset(50%);
    white-space: nowrap;
  }
  @media (width < 48rem), (pointer: coarse) {
    .font-toggle { min-block-size: 2.75rem; }
  }
</style>
